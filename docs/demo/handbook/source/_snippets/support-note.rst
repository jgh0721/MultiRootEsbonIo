.. note::

   This paragraph lives in ``_snippets/support-note.rst`` and is pulled in with
   the ``include`` directive. Included files are listed in ``exclude_patterns``
   so that Sphinx does not also treat them as standalone documents.
