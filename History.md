# 발자취

## v0.5.0

**Markdown(`.md`) 을 reStructuredText 와 같은 등급으로 지원합니다.**

* 구문 강조
  * 제목 여섯 단계, 굵게·기울임, 링크, 인라인 코드와 코드 블록, 인용문, 목록 마커,
    취소선, 수평선에 색이 붙습니다. 색은 설정 → 색상의 "TEXT Lexer Markdown" 에서
    항목별로 바꿉니다
  * 제목 줄은 `#` 뿐 아니라 글자까지 물듭니다. 제목 깊이는 색이 옅어지는 것과 굵기가
    빠지는 것 두 축으로 표시합니다
  * 제목 단위로 접힙니다. 코드 펜스 안의 `#` 이나 front matter 는 접기 머리가 되지 않습니다
* 좌측 요약
  * `#` 제목과 `===`/`---` 밑줄 제목을 계층으로 보여 줍니다. 코드 펜스 안의 `#`,
    front matter, 인용문 안의 제목은 올라오지 않습니다
  * 프로젝트 요약이 `.rest`/`.markdown`/`.mdown` 파일도 빠뜨리지 않습니다
* 프리뷰
  * `.md` 를 열면 **프로그램에 내장된 변환기**가 그립니다. 파이썬 환경이 준비되기 전에도
    바로 뜨고, 타이핑하면 곧바로 반영됩니다
  * 표, 작업 목록(`- [x]`), 취소선, 자동 링크, 각주, YAML front matter,
    GitHub 경고 상자(`> [!NOTE]`)를 지원합니다
  * 수식(`$...$`)은 KaTeX, 다이어그램은 mermaid 로 그립니다. 둘 다 인터넷에서 받아오므로
    설정의 "외부 리소스" 항목이 켜져 있어야 합니다 — reStructuredText 프리뷰와 같은
    규칙입니다. 문서에 그 문법이 없으면 아예 받지 않습니다
  * 편집기와 프리뷰가 양방향으로 스크롤 동기화되고, 프리뷰를 누르면 그 줄로 갑니다
  * 인터넷에서 변환기를 받지 못하면 내장본으로 물러섭니다. 프리뷰가 백지가 되지 않습니다
  * `myst-parser` 를 켠 Sphinx 프로젝트의 `.md` 는 예전처럼 실제 Sphinx 빌드로 그립니다.
    그 프로젝트의 테마와 확장, 상호 참조가 그대로 반영됩니다
* 그 밖에
  * 프리뷰 영역이 0 폭으로 시작해 "프리뷰가 없는 것처럼" 보이던 것을 고쳤습니다.
    핸들을 끌면 되살아나기는 했지만, 새 워크스페이스를 처음 열 때마다 그랬습니다
  * 어느 프로젝트에도 속하지 않는 단독 `.md` 는 이제 임시 Sphinx 프로젝트를 만들지
    않습니다. 파일 하나마다 임시 폴더와 파이썬 프로세스가 붙던 것이 사라집니다
  * 설정 → 프리뷰에 "수식 렌더러" 항목이 생겼습니다. "외부 리소스" 설명이 두 마크업을
    모두 설명하도록 다시 썼습니다

## v0.4.0

**이 버전부터 실행 파일 이름이 바뀝니다. 0.3.1 이하에서는 자동 업데이트가 되지 않으므로,
릴리스 페이지에서 전체 패키지를 받아 같은 폴더에 풀어 주세요.** 설정은 그대로 이어집니다.

* `.. image::` 등 경로를 인자로 받는 지시어에서 파일 목록 자동 완성
  * 디렉터리를 한 단계씩 따라 들어갈 수 있고, `../` 상대 경로와 `/` 로 시작하는
    Sphinx 절대 경로를 모두 지원합니다
  * 파일 이름만 알면 워크스페이스 어디에 있든 찾아 줍니다. 같은 이름이 여럿이면
    목록 오른쪽에 상대 경로가 함께 나옵니다
  * 자리에 맞는 확장자만 제안합니다 (이미지 자리에 `.rst` 를 제안하지 않습니다).
    빌드 산출물 폴더의 사본은 목록에 올라오지 않습니다
  * 오른쪽 상세 패널에 전체 경로와 이미지 미리보기, 형식·치수·크기를 보여 줍니다
* 자동완성 팝업의 결함 셋을 함께 고쳤습니다 — 글자를 칠 때마다 상세 패널이 껐다
  켜지던 것, LSP 응답이 오면 목록이 옆으로 튀던 것, 상세 패널이 목록을 덮을 수
  있던 것
* 자동완성 목록의 강조색을 테마에서 읽습니다 (다크 배경에서 대비가 모자랐습니다)
* 제품/실행 파일 이름 변경
  * `MultiRoot-reST-CPP.exe` -> `MultiRoot-reST Editor.exe`
  * `mrst_updater.exe` -> `MultiRoot-reST Updater.exe`
  * 설정 파일 `MultiRoot-reST-CPP.ini` -> `MultiRoot-reST Editor.ini`
    (첫 실행에서 이전 설정을 자동으로 옮겨 옵니다. 구 파일은 지우지 않습니다)
* 구버전에서 올라오는 자동 업데이트를 의도적으로 끊었습니다. 0.3.1 은 배포물에서
  자기 실행 파일 이름을 찾도록 만들어져 있어, 이름이 바뀐 패키지로는 제자리 교체가
  성립하지 않습니다. 받아 놓고 실패하는 것보다 처음부터 알리는 편이 낫다고 판단했습니다.
* 수동 설치 후 폴더에 남는 구 실행 파일 2개(`MultiRoot-reST-CPP.exe`,
  `mrst_updater.exe`)는 지워도 됩니다.
* 저장소에 한국어·영어 README 와, 화면을 찍는 데 쓴 데모 워크스페이스를 넣었습니다.
  앱 동작에는 영향이 없습니다.

## v0.3.1

* 도움말 → 정보 대화상자 추가 (아이콘, 현재/최신 버전, 저장소 주소)
* 타이핑 중에도 프리뷰가 미저장 편집을 반영 (doxygen 지시어는 자리표시자로 대체)
* 프리뷰 빌드 속도 개선 - 해석되지 않는 C++ 상호참조의 심볼 전체 스캔 제거
* 프리뷰 빌드 속도 개선 - 노드마다 하던 경로 해석을 메모이제이션

## v0.3.0

* 최초 커밋
