# 발자취

## 다음 판

**탐색기에서 파일을 찾고, 만들고, 빌드합니다.**

* 트리 위에 **필터 칸**이 생겼습니다. 이름의 일부만 쳐도 걸러집니다 — 앞머리가
  아니라 이름 어디든 맞으면 됩니다. `*.rst` 처럼 `*` 나 `?` 를 넣으면 와일드카드로
  봅니다. 걸린 항목의 상위 폴더는 함께 남고, 폴더 이름을 치면 그 안이 통째로
  보입니다. 필터를 지우면 필터를 걸기 **직전의 펼침 상태**로 돌아갑니다
* 창이 뜨면 키보드 포커스는 **문서**에 갑니다. 열린 문서가 없을 때만 트리로
  갑니다 — 필터 칸에 두면 무심코 친 글자가 문서 대신 필터를 채웁니다
* 필터 옆에 **새 파일 · 새 폴더 · 이름 바꾸기 · 삭제** 단추가 붙었습니다. 같은
  것들이 오른쪽 클릭 메뉴에도 있고, 거기에는 **파일 관리자에서 보기**가 더
  있습니다. 아이콘은 라이트 · 다크 테마의 색을 따라갑니다
* **헤더의 화살표가 이제 실제로 정렬합니다.** 예전에는 그려져 있기만 하고 눌러도
  아무 일이 없었습니다. 폴더가 언제나 파일 앞에 오고, `part2` 가 `part10` 앞에
  옵니다 — 이름을 글자 순으로만 세면 문서 번호를 읽을 수 없습니다
* **실제 Sphinx 프로젝트의 루트 폴더**를 오른쪽 클릭하면 **빌드(&B)…** 가
  나옵니다. 빌더와 결과물을 놓을 폴더를 고르면 그대로 `sphinx-build` 를 돌리고,
  진행 상황은 로그 탭으로 흐릅니다. "빌드가 끝나면 그 폴더를 탐색기로 열기" 를
  켜 두면 끝나는 즉시 열어 줍니다
* 빌더는 `html` · `dirhtml` · `singlehtml` · `latexpdf` · `epub` · `text` ·
  `man` 을 고르거나 직접 입력합니다. 프로젝트 환경에 Sphinx 가 없으면 내장
  환경으로 한 번 물러섭니다

**좌측 요약 탭에서 제목을 걸러 봅니다.**

* **활성 문서** 와 **프로젝트** 두 탭에 필터 칸이 생겼습니다. 부분 일치로
  거르고, 걸린 항목의 상위 항목은 함께 남으며 그 가지가 펼쳐집니다

**`|` 를 치면 문서의 치환 목록이 뜹니다.**

* 본문에서 `|` 를 치면 자동완성이 열립니다. 고르면 닫는 `|` 까지 함께 들어갑니다.
  `Ctrl+Space` 로도 부릅니다
* **`conf.py` 의 `rst_prolog` · `rst_epilog` 에 정의한 치환**이 함께 나옵니다.
  `|br|` 같은 조각을 두는 가장 흔한 자리인데 `.rst` 가 아니라 파이썬 소스 안이라
  지금까지는 편집기가 볼 수 없었습니다
* 지금 편집 중인 문서에 방금 적어 둔 정의도 **저장하기 전에** 나옵니다
* Sphinx 가 언제나 넣어 주는 `|version|` · `|release|` · `|today|` 도 있습니다.
  앞의 둘은 `conf.py` 에서 읽은 값을 함께 보여 줍니다
* 목록 옆 상세 패널이 **무엇으로 펼쳐지는지**(`raw:: html` 과 그 본문)와 어느
  파일 몇 번째 줄에 정의돼 있는지를 보여 줍니다 — 이름만으로는 `|br|` 과 `|pb|`
  중 어느 쪽인지 고를 수 없습니다
* 순서는 확실히 쓸 수 있는 것부터입니다. 이 문서 → `conf.py` → Sphinx 기본 →
  프로젝트의 다른 문서. 치환은 문서 단위라 다른 문서의 정의는 그 문서를
  `.. include::` 해야 쓸 수 있어서 뒤로 미룹니다
* 격자 표(`| 칸 | 칸 |`)와 줄 블록(`| 한 줄`)에서는 뜨지 않습니다. 거기서 뜨면
  표 한 칸마다 목록이 튀어나옵니다
* `.. |로고| ` 처럼 치환을 **정의**하는 자리에서는 그 자리에 올 수 있는
  directive(`replace` · `image` · `unicode` · `date` · `raw`)만 보여 줍니다

**좌측과 하단 패널을 Visual Studio 처럼 접어 둡니다.**

* 각 패널의 제목줄에 **핀 버튼**이 생겼습니다. 누르면 패널이 창 가장자리의 세로
  탭으로 접히고, 그 탭에 마우스를 올리면 편집기 위로 겹쳐 나옵니다. 벗어나면
  다시 접힙니다 — 넓은 문서를 볼 때 화면을 되찾는 가장 빠른 방법입니다
* 접히는 대상은 **탐색기**, **활성 문서**, **프로젝트**, 그리고 하단의 **진단** ·
  **로그** · **검색** 입니다. 예전에는 스플리터로 좁힐 수만 있어서 완전히 치울
  수 없었습니다
* 패널을 끌어 다른 자리로 옮기거나, 서로 탭으로 묶거나, 창 밖으로 떼어내 별도
  창으로 띄울 수도 있습니다
* 배치는 **워크스페이스마다 따로** 기억합니다. 다음에 그 워크스페이스를 열면
  핀으로 접어 둔 것까지 그대로 돌아옵니다
* 닫은 패널은 **보기 → 패널** 에서 다시 꺼냅니다
* `F11` 프리뷰 전체 화면에서 나오면 들어가기 **직전 상태**로 돌아옵니다. 어느
  패널을 핀으로 접어 두었는지, 하단에서 어느 탭을 보고 있었는지까지 지킵니다

## v0.5.2

**열어 둔 파일이 밖에서 바뀌면 알아챕니다.**

* 빌드 스크립트, `git checkout`, 다른 편집기가 파일을 고치면 편집기가 그 내용으로 다시
  불러옵니다. 캐럿과 스크롤 위치는 지킵니다
* **저장하지 않은 편집이 있는 탭은 먼저 묻습니다.** "자동 불러오기" 를 골라 두었더라도
  그렇습니다 — 그 설정으로 승낙한 것은 방금 친 글자를 잃는 것이 아니기 때문입니다
* 설정 → 텍스트 뷰어의 **외부 편집 인식** 에서 무시 / 자동 불러오기 / 사용자에게 묻기 중
  고릅니다. 기본값은 자동 불러오기입니다
* **감지 방식** 은 두 가지입니다. 기본값인 "파일 시스템 알림" 은 운영체제가 변경을 통보해
  주므로 기다리는 동안 비용이 없습니다. 알림이 오지 않는 네트워크 드라이브나 가상 파일
  시스템에서는 "폴링" 으로 바꾸고 간격을 정하십시오. 알림을 걸지 못한 파일은 설정을
  바꾸지 않아도 스스로 폴링으로 넘어갑니다
* 다른 창에서 일하다 돌아오는 순간에도 한 번 확인합니다. 알림이 오지 않는 자리에서
  마지막 그물이 됩니다
* 파일을 밖에서 지워도 탭을 닫지 않습니다. 편집 중이던 내용을 그대로 두고 "수정됨" 으로
  표시하므로, 탭을 닫을 때 평소처럼 저장을 묻습니다 — 그 버퍼가 마지막 사본일 수 있습니다
* 반쯤 쓰인 파일을 불러오지 않습니다. 큰 파일을 복사해 넣는 중에는 쓰기가 멎을 때까지
  기다립니다. 임시 파일을 만든 뒤 이름을 바꾸는 저장 방식(다른 편집기 다수가 그렇게
  합니다)도 삭제가 아니라 변경으로 봅니다
* 우리가 저장하는 동안 온 알림은 버립니다. 저장 직후에 "밖에서 바뀌었습니다" 가 뜨지
  않습니다

**프리뷰를 전체 화면으로 봅니다.**

* `F11` 을 누르면 프리뷰가 화면을 가득 채웁니다. 편집기와 좌측 트리·요약, 하단 진단이
  물러나고 문서만 남습니다. 다시 `F11` 을 누르거나 `Esc` 를 누르면 원래 배치로
  돌아옵니다 — 스플리터를 어디에 두었는지까지 그대로입니다
* 단축키는 설정 → 단축키의 **프리뷰** 항목에서 바꿉니다. 예전부터 `F5` 로 동작했지만
  표에 없어 바꿀 수 없었던 "프리뷰 다시 빌드" 도 같은 자리에 올라왔습니다

**`Ctrl+Tab` 으로 열린 문서를 넘나듭니다.**

* Visual Studio 처럼 문서 목록이 뜹니다. `Ctrl` 을 누른 채 `Tab` 을 더 치면 아래로,
  `Shift` 를 함께 누르면 위로 갑니다. `Ctrl` 을 떼면 그 문서로 갑니다
* 목록은 **최근에 본 순서**입니다. 그래서 한 번 눌러 떼면 직전 문서로 갑니다 —
  두 문서를 번갈아 보는 일이 한 손동작이 됩니다
* 전체 화면에서는 탭 바가 보이지 않으므로 이것이 문서를 옮기는 수단이 됩니다.
  프리뷰도 함께 따라갑니다

**편집기도 끝까지 접힙니다.**

* 편집기와 프리뷰 사이의 핸들을 왼쪽 끝까지 끌면 편집기가 사라지고 프리뷰만 남습니다.
  전에는 핸들을 끝까지 끌어도 편집기가 일정 폭 아래로 줄지 않았습니다

**단독 문서의 프리뷰 테마를 고릅니다.**

* `conf.py` 가 없는 문서는 임시 Sphinx 프로젝트로 미리보기하는데, 그 테마가
  `alabaster` 로 고정돼 있었습니다. 이제 설정 → 프리뷰의 **가상 프로젝트** 에서 고릅니다
* 기본값은 **"다른 프로젝트와 동일"** 입니다. 워크스페이스에 있는 실제 프로젝트
  `conf.py` 의 `html_theme` 를 그대로 써서, 프로젝트 밖의 문서도 같은 모습으로 보입니다
* Sphinx 내장 테마와 이 프로그램이 함께 설치하는 테마(`sphinx_rtd_theme`, `furo`,
  `pydata_sphinx_theme` 등)를 목록에서 고를 수도 있습니다. 설치되지 않은 테마를 고르면
  프리뷰 위에 설치 안내가 뜹니다

**요약 트리에 나타낼 깊이를 정합니다.**

* 왼쪽 **활성 문서** · **프로젝트** 트리가 문서의 모든 섹션 단계를 그대로 펼쳐 놓아,
  깊게 쪼갠 문서에서는 목록이 화면을 넘어갔습니다. 이제 설정 → 프리뷰의
  **개요 트리** 에서 몇 단계까지 나타낼지 정합니다. 기본값은 **3단계** 입니다
* 깊이는 섹션 단계로만 셉니다. 프로젝트 탭의 문서 줄은 세지 않으므로 두 탭이 같은
  단계의 섹션까지 보여 줍니다
* 깊이에 걸려 빠진 하위 항목이 있으면 그 줄 끝에 `…` 이 붙습니다. 마우스를 올리면
  몇 개가 빠졌는지 나옵니다 — 표시 없이 자르면 그 자리가 문서의 마지막 단계인 것처럼
  읽히기 때문입니다
* 값을 바꾸면 곧바로 반영됩니다. 문서를 다시 읽지 않고 이미 읽어 둔 개요로 다시
  그리므로, 문서가 수백 개인 프로젝트에서도 기다릴 것이 없습니다

## v0.5.0

**Markdown(`.md`) 을 reStructuredText 와 같은 등급으로 지원합니다.**

* 구문 강조
  * 제목 여섯 단계, 굵게·기울임, 링크, 인라인 코드와 코드 블록, 인용문, 목록 마커,
    취소선, 수평선에 색이 붙습니다. 색은 설정 → 색상의 "TEXT Lexer Markdown" 에서
    항목별로 바꿉니다
  * 제목 줄은 `#` 뿐 아니라 글자까지 물듭니다. 제목 깊이는 색이 옅어지는 것과 굵기가
    빠지는 것 두 축으로 표시합니다
  * 제목 단위로 접힙니다. 코드 펜스 안의 `#` 이나 front matter 는 접기 머리가 되지 않습니다
* 좌측 요약
  * `#` 제목과 `===`/`---` 밑줄 제목을 계층으로 보여 줍니다. 코드 펜스 안의 `#`,
    front matter, 인용문 안의 제목은 올라오지 않습니다
  * 프로젝트 요약이 `.rest`/`.markdown`/`.mdown` 파일도 빠뜨리지 않습니다
* 프리뷰
  * `.md` 를 열면 **프로그램에 내장된 변환기**가 그립니다. 파이썬 환경이 준비되기 전에도
    바로 뜨고, 타이핑하면 곧바로 반영됩니다
  * 표, 작업 목록(`- [x]`), 취소선, 자동 링크, 각주, YAML front matter,
    GitHub 경고 상자(`> [!NOTE]`)를 지원합니다
  * 수식(`$...$`)은 KaTeX, 다이어그램은 mermaid 로 그립니다. 둘 다 인터넷에서 받아오므로
    설정의 "외부 리소스" 항목이 켜져 있어야 합니다 — reStructuredText 프리뷰와 같은
    규칙입니다. 문서에 그 문법이 없으면 아예 받지 않습니다
  * 편집기와 프리뷰가 양방향으로 스크롤 동기화되고, 프리뷰를 누르면 그 줄로 갑니다
  * 인터넷에서 변환기를 받지 못하면 내장본으로 물러섭니다. 프리뷰가 백지가 되지 않습니다
  * `myst-parser` 를 켠 Sphinx 프로젝트의 `.md` 는 예전처럼 실제 Sphinx 빌드로 그립니다.
    그 프로젝트의 테마와 확장, 상호 참조가 그대로 반영됩니다
* 그 밖에
  * 프리뷰 영역이 0 폭으로 시작해 "프리뷰가 없는 것처럼" 보이던 것을 고쳤습니다.
    핸들을 끌면 되살아나기는 했지만, 새 워크스페이스를 처음 열 때마다 그랬습니다
  * 어느 프로젝트에도 속하지 않는 단독 `.md` 는 이제 임시 Sphinx 프로젝트를 만들지
    않습니다. 파일 하나마다 임시 폴더와 파이썬 프로세스가 붙던 것이 사라집니다
  * 설정 → 프리뷰에 "수식 렌더러" 항목이 생겼습니다. "외부 리소스" 설명이 두 마크업을
    모두 설명하도록 다시 썼습니다
* 편집기가 가벼워졌습니다
  * 한글이 든 문서를 열 때와 창 크기를 바꿀 때 잠깐 멈추던 것이 사라집니다. 편집기가
    글자 폭을 다시 재는 시간이 2,000줄 문서 기준으로 100밀리초에서 1.5밀리초가
    되었습니다. 자동 줄바꿈이 켜져 있으면 화면 밖 줄까지 다시 재기 때문에 문서가
    길수록 차이가 큽니다
  * ※ ○ → ℃ 「」 같은 기호가 섞여 있어도 마찬가지입니다
  * 등폭 글꼴에서 효과가 가장 큽니다 — Consolas, D2Coding, 나눔고딕코딩, Cascadia Code,
    굴림체 등. 맑은 고딕이나 Arial 같은 비례 글꼴에서는 달라지지 않습니다
  * 줄번호 글꼴을 본문과 다르게 지정해도 무시되던 것을 고쳤습니다

## v0.4.0

**이 버전부터 실행 파일 이름이 바뀝니다. 0.3.1 이하에서는 자동 업데이트가 되지 않으므로,
릴리스 페이지에서 전체 패키지를 받아 같은 폴더에 풀어 주세요.** 설정은 그대로 이어집니다.

* `.. image::` 등 경로를 인자로 받는 지시어에서 파일 목록 자동 완성
  * 디렉터리를 한 단계씩 따라 들어갈 수 있고, `../` 상대 경로와 `/` 로 시작하는
    Sphinx 절대 경로를 모두 지원합니다
  * 파일 이름만 알면 워크스페이스 어디에 있든 찾아 줍니다. 같은 이름이 여럿이면
    목록 오른쪽에 상대 경로가 함께 나옵니다
  * 자리에 맞는 확장자만 제안합니다 (이미지 자리에 `.rst` 를 제안하지 않습니다).
    빌드 산출물 폴더의 사본은 목록에 올라오지 않습니다
  * 오른쪽 상세 패널에 전체 경로와 이미지 미리보기, 형식·치수·크기를 보여 줍니다
* 자동완성 팝업의 결함 셋을 함께 고쳤습니다 — 글자를 칠 때마다 상세 패널이 껐다
  켜지던 것, LSP 응답이 오면 목록이 옆으로 튀던 것, 상세 패널이 목록을 덮을 수
  있던 것
* 자동완성 목록의 강조색을 테마에서 읽습니다 (다크 배경에서 대비가 모자랐습니다)
* 제품/실행 파일 이름 변경
  * `MultiRoot-reST-CPP.exe` -> `MultiRoot-reST Editor.exe`
  * `mrst_updater.exe` -> `MultiRoot-reST Updater.exe`
  * 설정 파일 `MultiRoot-reST-CPP.ini` -> `MultiRoot-reST Editor.ini`
    (첫 실행에서 이전 설정을 자동으로 옮겨 옵니다. 구 파일은 지우지 않습니다)
* 구버전에서 올라오는 자동 업데이트를 의도적으로 끊었습니다. 0.3.1 은 배포물에서
  자기 실행 파일 이름을 찾도록 만들어져 있어, 이름이 바뀐 패키지로는 제자리 교체가
  성립하지 않습니다. 받아 놓고 실패하는 것보다 처음부터 알리는 편이 낫다고 판단했습니다.
* 수동 설치 후 폴더에 남는 구 실행 파일 2개(`MultiRoot-reST-CPP.exe`,
  `mrst_updater.exe`)는 지워도 됩니다.
* 저장소에 한국어·영어 README 와, 화면을 찍는 데 쓴 데모 워크스페이스를 넣었습니다.
  앱 동작에는 영향이 없습니다.

## v0.3.1

* 도움말 → 정보 대화상자 추가 (아이콘, 현재/최신 버전, 저장소 주소)
* 타이핑 중에도 프리뷰가 미저장 편집을 반영 (doxygen 지시어는 자리표시자로 대체)
* 프리뷰 빌드 속도 개선 - 해석되지 않는 C++ 상호참조의 심볼 전체 스캔 제거
* 프리뷰 빌드 속도 개선 - 노드마다 하던 경로 해석을 메모이제이션

## v0.3.0

* 최초 커밋
